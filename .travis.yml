language: go
go:
  - 1.3
  - release
  - tip

install:
  - go get -v ./...
  - go get github.com/tools/godep
  - export PATH=$PATH:$HOME/gopath/bin

script:
  - godep get
  - godep save
  - godep go test -v ./...

